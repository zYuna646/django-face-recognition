version: '3.8'

services:
  web:
    build: .
    restart: always
    ports:
      - "8080:80"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./static:/app/staticfiles
      - ./upload:/app/media
    environment:
      - DEBUG=False
      - SECRET_KEY=${SECRET_KEY:-default_dev_secret_key}
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  static_volume:
  media_volume: 